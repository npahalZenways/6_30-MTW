import { MyDirectveDirective } from './my-directve.directive';

describe('MyDirectveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDirectveDirective();
    expect(directive).toBeTruthy();
  });
});
